# Ordered planets

This template demonstrates an ordered-list generator. The group entity is metadata; its members are
scheduled in the declared order. Ordered-list templates receive Entity references and use direct
fields such as `row.entity.label` rather than a data mapping.
