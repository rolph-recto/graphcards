# Scrambled planets

This template demonstrates a `scrambled_list` generator. The target entity is the solar system,
and the related planet entities appear in a shuffled order on the front of the card. The back shows
their configured order. The `target_label` and `item_label` slots select the displayed target and
planet names. The JSON, TOML, and YAML decks use the same exercise configuration.
