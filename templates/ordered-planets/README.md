# Ordered planets

This template demonstrates a missing-sequence-item generator. The group entity is metadata; its members are
scheduled in the declared order. The `row_label` slot supplies each visible list item and the
`answer` slot supplies the target answer. Templates use `row.entity.row_label` and `target.answer`.
