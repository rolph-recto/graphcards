# Ordered planets

This template demonstrates a missing-sequence-item generator. The group entity is metadata; its members are
scheduled in the declared order. Missing-sequence-item templates receive Entity references and use direct
fields such as `row.entity.label` rather than a data mapping.
