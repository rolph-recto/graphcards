# Capital choices

This template demonstrates a target-centered multiple-choice generator with a local distractor pool.
Its `question`, `choice_label`, and `answer` render slots select the target question, choice
labels, and answer fields.
