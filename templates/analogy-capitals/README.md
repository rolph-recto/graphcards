# Capital recall

This template demonstrates an entity-backed analogy generator. Each target entity maps to a source
entity. The generator uses the optional `render` slots `question` and `answer` for both `source`
and `target`; custom templates can use `source.question`, `target.answer`, and their `id` values.
